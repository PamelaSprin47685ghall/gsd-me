你目前的实现非常“强壮”，覆盖了大量的边缘情况、回退机制（Fallback）和完整的遥测日志（Lifecycle
UI），并且有严格的测试用例来守护。但正如你所说，现在的代码是一个典型的
“上帝文件”（God File），存在以下架构问题：

1.  职责耦合严重：事件监听、状态管理、UI 交互、定时器控制、正则匹配和业务重试逻辑全混在一个文件里。
2.  全局状态裸奔：state 对象在各个函数中被随意读写，追踪状态变更困难。
3.  调用链过长且硬编码：handleType1 失败后直接调用 handleType2，导致重试升级策略（Escalation）被硬编码在处理函数中。

为了在保持所有行为和测试通过的前提下，让架构变得清晰可维护，我为你制定了以下重构计划。

⚠️ 重要前提：关于测试用例的特殊性

你提供的 index.test.mjs 是通过读取 index.ts 的源码字符串做正则匹配来测试的（静态分析）。
如果你将代码拆分成多个文件，这些测试将全部失败！ 解决方案：

  - 方案 A（推荐）：引入 esbuild 或 tsup 进行打包，开发时写多文件（src/*），打包成一个
    dist/index.js，然后让测试脚本去读打包后的单文件。
  - 方案 B：保留单文件，但在文件内部使用类（Classes）或闭包模块进行严格的面向对象/模块化划分。

下面的架构设计适用于以上任何一种方案。核心思路是：分离副作用、纯化诊断逻辑、状态机化重试流程。

架构重构计划（分层设计）

我们将整个系统拆分为 5 个核心模块：

1. Configuration & Constants（静态配置层）

将所有不依赖运行时的常量、正则表达式移出核心逻辑。

  - 内容：PLUGIN, RETRY_LIMITS, MAX_NOTIFICATIONS, 所有 _RE 正则表达式。
  - 价值：配置一目了然，方便后续修改判定规则。

2. Runtime State Manager（状态管理层）

封装 state，禁止外部直接修改。

  - 职责：管理重试次数、定时器 Map、通知缓存。
  - 接口设计：
      - state.incrementRetry(type)
      - state.resetAllRetries(reason)
      - state.consumeAutoPauseSignal()
      - state.pushNotification(msg)
  - 价值：状态变更收敛，所有状态重置或增加的动作都在这里发生，杜绝状态不一致。

3. Diagnostics & Classifier（诊断与分类器层 - 纯函数）

负责“诊断出现了什么错误”。

  - 职责：接收 combinedLog 和 stopReason，返回具体的错误类型。
  - 设计：
    function classifyError(reason: string, combinedLog: string): ErrorType {
      if (classifyAsSchemaOverload(combinedLog)) return "schema_overload";
      if (NETWORK_RE.test(combinedLog)) return "type1_network";
      if (classifyAsType2Provider(combinedLog)) return "type2_provider";
      if (TOOL_INVOCATION_PASSTHROUGH_RE.test(combinedLog)) return "type0_tool";
      return "type3_blocker";
    }
  - 价值：剥离复杂的 if-else，诊断逻辑可以脱离 GSD API 独立测试。

4. Retry Scheduler & Escalator（重试与升级调度层）

负责“执行具体的重试操作以及升级逻辑”。

  - 包含组件：
      - TimerManager: 封装 scheduleRetryTimer, cancelRetryTimer。
      - ActionDispatcher: 封装 safeSendUserMessage,
        safeRetryLastTurn，处理降级（Fallback）。
      - EscalationEngine: 将当前的 handleTypeX 转换为策略模式。如果 Type1
        次数用尽，调度器负责将其转交（Escalate）给 Type2，而不是让 Type1
        去调用 Type2。

5. Extension Hub (事件总线层 / index.ts 入口)

负责将 GSD 的 Hook 映射到内部的控制器。这个文件应该变得非常薄，就像一个目录一样。

重构后的主入口效果预览 (index.ts 雏形)

重构后，你的 registerExtension 应该长这样，逻辑完全自解释：

export default async function registerExtension(pi: ExtensionAPI) {
  const logger = new LifecycleLogger(PLUGIN);
  const state = new RuntimeState(logger);
  const scheduler = new RetryScheduler(pi, state, logger);
  const handler = new RecoveryHandler(pi, state, scheduler, logger);

  logger.log("factory_registered", { reason: "extension_factory_initialized" });

  pi.on("session_start", (_event, ctx) => {
    logger.bindUi(ctx);
    logger.log("hook_session_start", { reason: "session_start" });
    logger.systemNotify("🚀 [AutoContinue] Verbose lifecycle mode enabled...");
  });

  pi.on("stop", (event: StopEvent) => {
    const errorMsg = handler.extractStopError(event);
    const combinedLog = state.consumeDiagnostics(errorMsg);
    
    logger.log("hook_stop", { reason: event.reason, detail: combinedLog });

    // 1. 处理人为干预 / Cancelled
    if (handler.checkUserIntervention(event.reason, combinedLog)) {
        return handler.standDown("stop:user_intervention_detected");
    }

    // 2. 检查 AutoPause 信号
    if (!state.hasAutoPauseSignal() && !state.isFixingType3) return;

    // 3. 诊断错误类型
    const errorType = Classifier.classify(event.reason, combinedLog);

    // 4. 路由到对应的处理策略
    handler.executeRecovery(errorType, event.reason, combinedLog);
  });

  // 其他 hooks (agent_end, turn_end, notification) 也同样变薄，只负责组装数据并调用 state/handler
}

实施路线图（推荐的四步重构法）

如果你直接大改，很容易把行为弄坏，导致测试红一片。建议按照以下顺序进行重构（每次改动后都跑一遍 node --test index.test.mjs）：

第一步：提取 Logger 和 UI 代理

  - 创建一个 LifecycleLogger 类，把 logLifecycle, notifyLifecycleUi, bindUiNotifier
    全塞进去。
  - 把 pi.sendMessage (系统消息部分) 封装进这个类。
  - 测试验证：遥测字符串是否还存在。

第二步：封装 RuntimeState 和 TimerManager

  - 将全局变量 state 变成一个 Class。
  - 将涉及 clearTimeout 和 setTimeout 的 cancelRetryTimer 和 scheduleRetryTimer 封装为
    TimerManager 放入类中。
  - 提供 resetRetries, standDown 等对外方法。
  - 测试验证：定时器逻辑和重置逻辑的测试是否通过。

第三步：抽象 Action Wrapper

  - 提取 safeSendUserMessage 和
    safeRetryLastTurn。这两个函数很独立，主要是做异常捕获和向下兼容（fallback），把它们提取成一个单独的工具对象（比如
    GsdApiAdapter）。
  - 测试验证：fallback 的 triggerTurn 和 missing 错误捕获是否正常。

第四步：解耦 Handler 和 Escalation (最难的一步)

  - 现在的 handleType1 结尾直接调用了 handleType2。
  - 重构方案：让 handleType1 返回一个状态（例如 SUCCESS, EXHAUSTED）。
  - 创建一个 RecoveryEngine：
    if (type === 'type1') {
       const result = handleType1();
       if (result === 'EXHAUSTED') {
           // 外部进行升级，而不是函数内部嵌套
           handleType2(triggerReason: "type1_exhausted", escalation: "type1_to_type2");
       }
    }

第五步：收尾

  - 清理那些冗长重复的字符串字面量（比如 [AutoContinue] 开头的提示语），放到常量对象里。
  - 在保证功能完全一致的情况下，调整 registerExtension，使其变得像上面我写的预览代码那样清爽。

总结

你现在的代码是面向过程写法的极致体现，防线设得很死，但代码呈现网状结构。通过引入面向对象（封装状态）和单一职责（抽离分类器和触发器），你可以完美保持原有的鲁棒性，同时让这段代码在
S03、S04 阶段非常容易进行扩展。

非常赞同！**“测试应该测行为（验证黑盒的输入输出和副作用），而不是测实现（白盒的源码字符串匹配）”**，这是软件工程的黄金准则。

当前的测试（通过读取源码字符串做正则匹配）是典型的**静态结构断言**。这种测试最大的痛点就是：**它锁死了代码的结构**。你哪怕把变量改个名字、拆成两个文件、或者换一种设计模式，代码行为完全没变，测试也会瞬间全红。

只有把测试重构为**基于行为的测试（Behavior-Driven）**，你才能彻底放开手脚去重构 `index.ts`。

下面是针对该扩展的**测试重构与架构重构联合计划**。

---

### 第一阶段：重构测试框架（搭建 Test Harness）

要测试扩展的行为，我们需要模拟（Mock）GSD 的运行环境（也就是 `ExtensionAPI` 对象），并拦截扩展发出的指令（`sendMessage`, `sendUserMessage` 等）。

#### 1. 打造 Mock 运行环境 (Test Harness)
我们需要在 `index.test.mjs` 中创建一个拦截器，用来模拟 GSD 宿主。

```javascript
import test, { mock } from "node:test";
import assert from "node:assert/strict";
import registerExtension from "./index.ts"; // 直接引入代码，而不是读取字符串

// 每次测试前生成一个干净的沙箱环境
function createSandbox() {
  const listeners = {};
  const uiNotifyMock = mock.fn();
  
  const pi = {
    // 模拟事件注册与触发
    on: (event, handler) => { listeners[event] = handler; },
    emit: (event, ...args) => { if (listeners[event]) listeners[event](...args); },
    
    // 模拟 API 调用（使用 mock 追踪调用记录）
    sendMessage: mock.fn(),
    sendUserMessage: mock.fn(),
    retryLastTurn: mock.fn(),
  };

  const ctx = {
    ui: { notify: uiNotifyMock },
    abort: mock.fn()
  };

  return { pi, ctx, listeners, uiNotifyMock };
}
```

#### 2. 引入时间旅行（Fake Timers）
代码里有大量 `setTimeout`（比如等待 5 秒、指数退避到 30 秒）。在行为测试中，我们不能真等，必须使用 `node:test` 自带的定时器模拟。

---

### 第二阶段：将“正则匹配”转换为“行为断言”（对比示例）

我们挑几个典型的现有测试，看看如何转换为真正的行为测试。

#### 案例 A：测试 Type 2 (Provider Pause) 的重试行为
*   **原来的实现测试（读字符串）**：查代码里有没有 `classifyAsType2Provider`，有没有 `if` 语句。
*   **现在的行为测试（测黑盒）**：模拟收到 `provider error` 通知，模拟停止，快进 5 秒，断言是否触发了 `/gsd auto`。

```javascript
test("Behavior: Type2 provider errors trigger a 5s cooldown and resume auto-mode", async (t) => {
  mock.timers.enable({ apis: ['setTimeout', 'clearTimeout'] });
  const { pi, ctx } = createSandbox();
  
  // 1. 初始化扩展
  await registerExtension(pi);
  pi.emit("session_start", {}, ctx);

  // 2. 模拟行为：收到 Provider Error 通知，随后 Agent 停止
  pi.emit("notification", { message: "rate limited from provider", kind: "error" });
  pi.emit("stop", { reason: "error", lastMessage: {} });

  // 3. 立即断言：发送了系统提示，但还没发送继续指令（因为在等 5 秒）
  assert.equal(pi.sendMessage.mock.calls.length, 2); // 1次启动提示 + 1次Type2提示
  assert.equal(pi.sendUserMessage.mock.calls.length, 0);

  // 4. 快进时间 5000 毫秒
  mock.timers.tick(5000);

  // 5. 最终断言：时间到了，是否自动发出了恢复指令？
  assert.equal(pi.sendUserMessage.mock.calls[0].arguments[0], "/gsd auto");
  
  mock.timers.reset();
});
```

#### 案例 B：测试 Schema Overload (工具验证失败) 回退机制
*   **原来的测试**：匹配 `if (!classifyAsSchemaOverload...` 等字符串。
*   **现在的行为测试**：触发超过最大重试次数时，验证系统是否正确“放弃干预”（Stand Down）。

```javascript
test("Behavior: Stops retrying schema-overload when cap is reached", async (t) => {
  // 假设配置了最大重试 1 次
  process.env.GSD_AUTO_CONTINUE_SCHEMA_OVERLOAD_MAX_RETRIES = "1";
  
  const { pi, ctx } = createSandbox();
  await registerExtension(pi);
  
  // 第一次触发 Schema overload
  pi.emit("stop", { reason: "error", lastMessage: { errorMessage: "schema overload" } });
  
  // 断言：调度了重试 (调用了 retryLastTurn)
  mock.timers.tick(1500);
  assert.equal(pi.retryLastTurn.mock.calls.length, 1);

  // 第二次触发（达到上限）
  pi.emit("stop", { reason: "error", lastMessage: { errorMessage: "schema overload" } });
  
  // 断言：应当发出要求人为干预的通知，且不再重试
  const lastSysMsg = pi.sendMessage.mock.calls.at(-1).arguments[0].content;
  assert.match(lastSysMsg, /Manual intervention required/);
  assert.equal(pi.retryLastTurn.mock.calls.length, 1); // 次数依然是1，没增加
});
```

---

### 第三阶段：代码结构重构 (The Final Refactoring)

当你把所有的 `assert.match(source, /.../)` 全部改写为上面的**行为测试**，且全部跑通（绿灯）后，**你就可以把 `index.ts` 删了重写，只要测试还绿，你的重构就是 100% 安全的。**

按照之前提到的架构，你可以放心大胆地把代码切分成多个模块（由于是 Node 环境，推荐直接用 ES 模块，或者用构建工具打包）：

**文件结构规划：**
```text
src/
 ├── constants.ts         # 放所有的正则、常量
 ├── state.ts             # RuntimeState 类，管理重试次数和定时器
 ├── diagnostics.ts       # 纯函数：根据 errorMessage 判定是 Type1/2/3
 ├── actions.ts           # 封装 api (safeRetryLastTurn, safeSendUserMessage)
 ├── recoveryStrategies.ts# 具体的 Type 1/2/3 升级与处置逻辑
 └── index.ts             # 组装层：只负责 pi.on 绑定和调用上述模块
```

**重构后的 `src/index.ts` 样例（极简且语义化）：**

```typescript
import { RecoveryEngine } from './recoveryStrategies';
import { StateManager } from './state';
import { diagnoseError } from './diagnostics';

export default async function registerExtension(pi: ExtensionAPI) {
  const state = new StateManager();
  const engine = new RecoveryEngine(pi, state);

  pi.on("stop", (event) => {
    // 1. 提取错误信息
    const errorMessage = extractStopError(event);
    const logContext = state.consumeLogs(errorMessage);

    // 2. 检查人工打断
    if (isUserIntervention(logContext)) {
      return engine.standDown("user_intervention");
    }

    // 3. 诊断错误类型 (纯函数测试)
    const errorType = diagnoseError(event.reason, logContext);

    // 4. 派发恢复策略
    engine.execute(errorType, event.reason, logContext);
  });
  
  // ... 其他 hook 同理
}
```

### 总结行动路线：
1. **别动业务代码**：先保留现有的 `index.ts` 不动。
2. **消灭字符串测试**：引入 `node:test` 的 `mock` 和 `timers`，重写 `index.test.mjs`，把你原来列的 10 个测试点全翻译成触发事件、检查 `sendMessage` 被调用的黑盒测试。
3. **肢解上帝文件**：测试全绿后，新建 `src` 文件夹，将面条代码拆解、类化。
4. **见证奇迹**：运行重构后的测试，如果绿了，说明你完成了架构的升华，且没有丢失任何边缘 case（Behavior Preserved）！

